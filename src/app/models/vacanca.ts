import { SafeUrl } from '@angular/platform-browser';

export interface Vacanca {
    id: string;
    titol: string;
    ciutat: string;
    hotel: string;
    preu: string;
    comentaris: string;
    img: string;
}
