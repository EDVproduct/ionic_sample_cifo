import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jrbistuer.cifo',
  appName: 'cifo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
